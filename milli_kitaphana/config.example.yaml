aes:
  # AES key used to encrypt the private RSA key
  raw_key: <SET ME>
  default_iv: <SET ME>
  # Salt used in AES encryption
  salt: <SET ME>

# Server RSA private key encrypted with AES and represented in base64.
priv_key: <SET ME>

# Prefix added in front of all signatures.
sig_prefix: <SET ME>

# TLS-enabled Aiven URI for the insert-only books_crawler user.
database_url: <SET ME>

yandex:
  disk:
    oauth_token: <SET ME>
    target_dir: <SET ME>
